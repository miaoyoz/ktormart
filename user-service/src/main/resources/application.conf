ktor {
    deployment {
        port = 8081 # Port for user-service
    }
    application {
        modules = [ com.ktormart.userservice.ApplicationKt.module ]
        serviceName = "user-service"
    }
}

# Custom application configuration for database
# database {
#     driverClassName = "org.postgresql.Driver"
#     jdbcURL = "jdbc:postgresql://localhost:5432/user_db"
#     username = "postgres"
#     password = "123456"
#     maximumPoolSize = 10
# }

consul{
    host = "127.0.0.1"
    port = 8500
}
